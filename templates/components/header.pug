mixin item(title, url)
  li(class=(page == title ? 'current-menu-item' : ''))
    a(href=url)= title

header.page-header
  .page-header__inner.container
    a.page-header__logo(href="/")
    button.hamburger
      span Menu
  nav.page-header__nav
    ul
      +item('Home', '/')


